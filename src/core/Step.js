export default class {
    constructor (state) {
        this.state = state;
    }

    getType () {
        let state = this.state
    }

    sort () {
        this.sort((a, b) => {
        })
    }
}